# Pulling workspace (both Binos and IOS) and pick from one of ref points
mist new -p mcp_cable --skip-init

# To build (in workspace root)
mcp_ios_precommit ubrsim_super
mcp_ios_precommit ubr_super

# To update (in binos)
# Also need to update ios clearcase view
acme update -workspace -devline mcp_cable_binos 
cc_update -m LATEST

# To remove view (in workspace root)
mist clean

