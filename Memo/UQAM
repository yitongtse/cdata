How to measure system idle time?
Do "sin" on QNX console.  The rightmost number tells you the number of ms
a task spends since boot up.  The procnto is the idle task.


Embedded CLI for Ferrari:
  input docsis qam <qam_id> udp <dest_udp>
  input docsis qam <qam_id> off
  input docsis dscp <begin_dscp> {<end_dscp>}

  input show gige
  input show marvell

  input show docsis
  input show docsis udp
  input show docsis qam
  input show docsis qam <qam_id>
  input show docsis interrupt
  input show track
  input track udp <dst_udp>
  input track qam <qam_id>
  input track pid <pid>


----------------------------------------
To configure WB using QNX CLI:

cli_process
input docsis qam 23 udp 50000
input docsis dscp 1 2

----------------------------------------
To allow WS tftp download:

video 4 switch vlan 202 debug
int g4/16
switchport access vlan 202
do show vlan

