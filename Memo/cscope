# Setting cscope using ~/Script/snaps (for flo_gsbu8_young_rfgw)
cd /vob/ios
snaps -a

# To build cscope database
cscope -q

# To use cscope
cscope -d



-------------------------
To acess rfgw cscope database:

cd /auto/ios-snaps2/snaps/flo_rfgw/ios

cscope -d

-------------------------
# Set up cscope for a directory
cscope -b -R
cscope -b -R -s <top_src_dir>

-------------------------
# For latest mcp_cable branch, use this instead:
which cscope-64
/router/bin/cscope-64

