tclsh

expr 1 + 2	; expression evaluation

// variables
set x 2		; set x to 2
set x		; print value of x
set cmd expr

// variable subsitition
expr $x+1	
$cmd $x * $x

// command subsition
set x 6
set y 7
set z "$x + $y is [ expr $x + $y]"
