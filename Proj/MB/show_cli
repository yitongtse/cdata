- Scope:
  Chassis, Qam domain, Linecard, Qam channel

- Overview:
  - session count by status
  - total bitrate

- Session info:
  - input signature: UDP - dst_udp, ASM - dst_ip, SSM - src_ip + dst_ip
  - input program number(s)
  - session type: mux, passthru, data
  - output: qam channel, program number(s)
  - session status: OFF, IDLE, ACTIVE, BLOCKED: PSI
  - session bitrate

- Organized by:
  - session status
  - input types: UDP, ASM, SSM
  - session type


show cable video session {qam-domin <id> | slot <id> | <qam_chan> }

-------------------------------------------------
- Detailed info:
  - Per session:
    - IP packet counts
    - TP counts
    - error counts
    - times: duration, idle time
    - input PAT / PMT
    - output PMT

  - Per QAM
    - output PAT
    - TP counts
    - error counts


show cable video session <in_id>
show cable video <qam_chan>

-----------------------------------------------

LC-CLI:
  video show session 
  video show session <out_id>
  video show session summary
  video show session active
  video show session idle
  video show session off
  video show session block
  video show session psi-ready
  video show session unicast
  video show session asm
  video show session ssm
  video show session qam <qam_id>


Detailed show:
  video show session <out_id>

One liner show:
  video show session <scope> <filter>

  scope:
    - all
    - qam <qam_id>

  filter:
    - {none}
    - active
    - idle
    - off
    - block
    - psi-ready
    - unicast
    - asm
    - ssm

Summary show:
  video show session summary

---------------------------------------------

video show session [qam <qam_id>]
      [summary | off | idle | active | psi-ready | block | unicast | asm | ssm]


---------------------------------------------

Latest video show CLIs:

show cable video label
show cable video label <label>

show cable video multicast uplink
show cable video multicast uplink <interface>

show cable video packets <qam_chan>
show cable video packets all

show cable video route unicast all
show cable video route unicast slot <n>
show cable video route unicast qam-domain <n>
show cable video route multicast all
show cable video route multicast slot <n>

show cable video server-group all
show cable video server-group mapping
show cable video server-group <name>

show cable video session all
show cable video session all brief
show cable video session all brief {active | asm | blocked | bound | data
                                    | idle | mux | off | passthru | psi
                                    | shell | ssm | upd}
show cable video session id <n>
show cable video session id <n> in
show cable video session id <n> out

show cable video session <qam_chan>
show cable video session <qam_chan> brief {...}
show cable video session <qam_chan> summary

show cable video session qam-domain <qd>
show cable video session qam-domain <qd> brief {...}
show cable video session qam-domain <qd> summary

show cable video session slot <n>
show cable video session slot <n> brief {...}
show cable video session slot <n> summary

show cable video ssp gqi
show cable video ssp sdv

show cable video statistics packet all brief
show cable video statistics packet all detail

show cable video statistics packet slot <n> brief
show cable video statistics packet slot <n> detail
