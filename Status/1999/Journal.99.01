1/25/99
  Bufferer is successful in creating first picture's PicStat.
  Tips: Use -Wimplicit compiler option to warn for implicitly defined functions,
        and make sure all function prototypes are defined before use.
        That will save a lot of trouble.

1/26/99
  Make all packets size 256.
  File to file bypass mode running, however, program stops after Frame 125.
  
1/27/99
  The SUN version of clrU has problem!
  Need to check new picture operation in bufferer
  Bugs to fix:
    - PID replacement: Done
    - PTS/DTS parsing: Done
    - 250 frame hang: hang is due to the loss of pidBuf.
      It seems like it is NOT lossing buffers, but having a longer delay
      between buffering and switching!!
      Bug: wrong TP counting for 1st picture!
    - 512 frame bug: 10 MB in output file.
      picStatPool is in an error status: last element has 0 pointer!

1/28/99
  512 frame bug: Done
    - the bug seems to be in the memcpy.  If disable, successful in
      parsing the whole input file.
    - bug: the size in #define should be in brackets!

  Fixed bug in tsviewer: out of sync when payload size < 16!

  Created a bitstream combining echo2 and echo3.

  **** Live feed bypass path works!
  **** Ad channel dropping path works!
  **** Other channel bypass path works!

  Successful in parsing sequence, GOP and picture headers to get
      frame_rate_code, closed_gop_flag, tr and picture_type

1/29/99
  To do:
     splice command generation and processing: Done
     parse picture coding extension to get repeat_first_field: Done
     adjust time base according to local clock: 
     perform switching!

